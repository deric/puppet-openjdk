appveyor.yml:
  delete: true
Gemfile:
  required:
    ':development':
      - gem: 'librarian-puppet'
        platforms: ruby
.gitignore:
  paths:
    - '.tmp/'
    - '.librarian/'
    - 'Puppetfile.lock'
